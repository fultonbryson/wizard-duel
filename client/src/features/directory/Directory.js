import React from "react";

import { PageHeader } from "../pageHelpers/pageHelpers";

const Directory = () => {
  return (
    <div className='directory'>
      <PageHeader className='directory__header' header='DIRECTORY' />
    </div>
  );
};

export default Directory;
